# 未完剧场用户手册

适用版本 **V2.10.5（2.10.5+2105）**。本次按当前源码中的界面、按钮和操作流程核对，更新于 2026 年 9 月 11 日。

你负责决定下一步，AI 负责接着演。第一次来，先读前两章，把第一回合跑通；剩下的功能可以等剧情把你留住以后再慢慢翻。

安装、源码运行和自行打包见 [README](README.md)，版本变化见 [更新记录](CHANGELOG.md)。这份手册讲当前怎么用，不把旧版公告混进操作步骤。

## 目录

1. [第一次开演](#第一次开演)
2. [找到常用入口](#找到常用入口)
3. [创建与管理剧场](#创建与管理剧场)
4. [聊天与消息操作](#聊天与消息操作)
5. [专属玩法与游戏面板](#专属玩法与游戏面板)
6. [固定地图与大型群聊](#固定地图与大型群聊)
7. [故事工作区与番外](#故事工作区与番外)
8. [NPC 私聊与角色卡](#npc-私聊与角色卡)
9. [带走 NPC](#带走-npc)
10. [用户人设与世界书](#用户人设与世界书)
11. [长期记忆与生成设置](#长期记忆与生成设置)
12. [啥币与小游戏中心](#啥币与小游戏中心)
13. [主题与气泡](#主题与气泡)
14. [备份迁移与恢复](#备份迁移与恢复)
15. [数据保存与隐私](#数据保存与隐私)
16. [遇到问题先看这里](#遇到问题先看这里)
17. [转载修改与交流](#转载修改与交流)

## 第一次开演

### 连接 AI 服务

1. 打开应用，按住开场帷幕或底部提示条，向一侧拖开。
2. 进入「设置」，找到「API 设置」。
3. 填写「API 地址」「API Key」和「模型名称」。地址可以是服务商提供的接口地址或站点根地址，需兼容 OpenAI Chat Completions。
4. 可以点击「拉取模型」选一个模型；服务商不支持模型列表时，按其说明手动填写名称。
5. 点击「测试连接」，查看结果，再点击「保存 API 设置」。测试成功不等于已经保存。

应用不附送共享 API Key。模型服务可能收费，具体额度和价格由你选的服务商决定。剧场里的啥币不能抵扣模型账单，先替钱包解释清楚。

优先使用 HTTPS。「允许当前主 API 使用明文 HTTP」仅适合你信任的自建服务，打开后密钥和剧情可能以明文传输。网页端还需要服务商允许跨域请求。

### 完成第一回合

可以先选择「第一次开幕」，跟幕灯体验故事；也可以到「角色」创建自己的剧场。

1. 在聊天输入框写一句台词或一个行动。
2. 点击「发送」，把它加入本回合。需要补充时，可以继续写、继续发送。
3. 点击右侧小飞机，AI 才会读取本回合内容并生成回复。
4. 等回复完成，再选择行动或继续自己输入。

**“发送”和小飞机分两步工作。** 如果消息已经出现在聊天里，AI 却还没接话，先看看小飞机有没有点。它在等你把这一回合交齐。

生成过程中，小飞机所在的按钮可用于暂停。暂停会中断当前请求，不会退回已经产生的模型用量。想继续体验教程，可以在「设置」的「教程与帮助」中选择「重新查看快速上手」或「完整功能教程」。

## 找到常用入口

宽屏布局在左侧显示「对话」「角色」「NPC」「用户」「设置」五个入口。窄屏布局的底栏显示「对话」「角色」「NPC」「设置」四个入口，用户人设从聊天页顶部的「用户」按钮进入。布局会随窗口宽度变化。

| 要做的事 | 入口 |
| --- | --- |
| 继续当前剧情 | 对话 |
| 创建、切换或编辑故事 | 角色 |
| 查看当前世界人物并私聊 | NPC |
| 管理自己在剧情里的身份 | 用户 |
| 配置服务、备份和显示效果 | 设置 |
| 查前情、人物、线索或番外 | 聊天页的故事 |
| 看当前状态、任务和玩法 | 聊天页的游戏面板 |
| 领取奖励、看背包和装扮 | 小游戏中心 |

聊天页会根据屏幕宽度收起部分按钮。找不到导出、分享卡或其他操作时，打开「功能菜单」。全局搜索可以查角色、消息、书签、NPC、世界书和已保存的作品。

## 创建与管理剧场

### 自己编写

在「角色」页打开新建入口，选择「自己编写」。填写名称、可选简介、开场白和「角色提示词」，也可以上传头像。提示词用来说明人物、世界规则、叙事风格和不希望出现的内容。

不要指望一句“永不跑偏”能包治百病。把你在意的事实写具体，生成后仍可以修正角色和故事。

点击「上传文件」可以导入支持的设定文档，导入后检查内容再保存。按钮旁会说明当前支持的格式，文档导入不等于恢复游戏存档。恢复存档应去「设置」里的「导入数据」。

### 让 AI 帮你写

1. 切到「帮你写」，选择「完整模拟器」或「只生成世界观」。
2. 填写「文游灵感」，按需补充风格和额外限制。角色名称可以先留空。
3. 点击「生成提示词」，或者通过「开盲盒」获取随机创意。
4. 生成过程中会出现实时文字预览。完成后，名称、简介、开场白和系统提示词会分到各自的输入框。
5. 检查并修改结果，再点击「创建角色」。生成完还不等于角色已创建。

当前界面也保留了「开盲盒（怀旧版）」。自己编写的设定可以用「润色你的人物」扩展，结果仍由你决定是否采用。

生成内容截断或格式不完整时，应用会尝试补全或修复；仍失败会显示错误。不要把刚出现的半段预览当成最终结果保存。

### 模型与故事模式

角色编辑器可以调整 Temperature、Top P，并设置「流式输出」「分段输出」「下一步选项」。参数影响表达方式，但不会让所有模型都表现一致。

「固定地图主线模式」和「大型群聊模式」会改变玩法。已保存角色开启后，不能直接退回普通模式，开启前请先备份；具体区别见后面的模式章节。

### 切换、编辑与删除

每个剧场有自己的聊天、记忆、NPC 和进度。切换角色就是切换故事，不会把所有人的剧情塞进同一个聊天框。

在角色卡的详情和操作入口中编辑或删除角色。编辑后的设定用于后续生成，旧消息不会自动重写。删除前会有确认和自动保护快照；误删后可以查看「恢复中心」，但快照也保存在本地，不能代替导出备份。

## 聊天与消息操作

### 选项和自定义行动

开启「下一步选项」后，AI 可以给出行动建议。选项区可以展开查看，你也可以完全自己输入，不需要每回合都从建议里挑。

选项支持预演，预演结果不推进主线。预演依然需要调用 AI，别把它当成不用额度的免费彩排。

### 修改已有消息

消息支持复制、编辑、删除、重新生成、书签以及创建剧情分支。手机端可以长按消息打开操作菜单。

重新生成时可填写本次方向，例如希望语气收敛或剧情换个走向，留空则普通重生。改完历史消息后，后续生成会根据新的上下文继续；已经存在的后续段落不会被自动全部改写。

收藏重要消息时可以加书签备注，之后从搜索或继续看板找回来。长聊天可以快速回到顶部或底部。

### IF 线与导演指令

IF 线从选定进度创建独立分支，之后可沿另一种选择继续，不覆盖原分支。它和把当前进度恢复到旧保存点是两回事，具体见「备份迁移与恢复」。

输入框左侧可以打开「剧情导演台」，设置下一回合的导演补充。这里适合交代本轮想强调的方向；需要长期保持的世界规则放进世界书。

### HTML 面板与分享

普通模式下，回复可能带状态卡、论坛或其他 HTML 小面板。部分面板支持交互；较早的面板可能默认折叠，点开后再加载。模型未遵循格式时，不一定能显示成预期的样子。

网页版和安卓版的 HTML 预览实现不同，浏览器效果不能直接当作安卓端验收。大型群聊模式也不会按普通模式生成 HTML 美化框。

通过「功能菜单」的「导出消息记录」进入勾选模式，选择消息后导出。需要分享一段剧情时，可以使用「剧情分享卡」。HTML 导出和分享卡适合阅读展示，恢复可继续游玩的进度请用 JSON 存档。

## 专属玩法与游戏面板

角色创建后，在角色详情的「玩法系统」区域点击「生成玩法系统」。AI 会按当前设定生成适合这个故事的变量和规则。校园故事可以关心压力，调查故事可以关心线索，不需要人人都背着同一套血条。

生成后可点击「打开玩法系统」，查看两个视图。

| 视图 | 用途 |
| --- | --- |
| 玩家面板 | 查看玩家可知的状态、数值或模糊阶段 |
| 幕后参数 | 查看创作和调试所需的隐藏信息、变量与规则 |

变量会区分公开、模糊、导演和引擎可见级别。应用按已声明的类型、范围和权限处理 AI 提交的变化，不会把任意一段文字都当作有效数值修改。

聊天页的「游戏面板」用于查看状态和任务。玩法变量会随游戏状态、消息快照、分支和存档保存。需要「重新生成」玩法系统时，先保存快照，避免在长篇中途无准备地更换规则。

## 固定地图与大型群聊

### 固定地图主线

在角色编辑器开启「固定地图主线模式」并确认。开局由 AI 生成一次地图蓝图，选定出生点后，通过地点、路线和行动篮子推进。

- 地点和道路组成固定的连通、双向有环地图，后续不会每回合重新抽一张地图。
- 每回合基础行动力为 3 AP，临时上限为 5 AP，总上限为 8 AP。
- 自然恢复不超过临时上限；允许突破的恢复道具也受总上限约束。
- 移动与行动先由本地规则结算 NPC、任务、事件和危机变化，再由 AI 根据结算结果续写剧情。

开启后的地图是这个角色的主线玩法。已保存开启的角色不能直接关闭地图模式，想尝试前请先导出或保留一份普通模式的角色。

### 大型群聊

「大型群聊模式」让主线通过旁白和当前场景 NPC 的群聊气泡展开。它保留状态面板和随机 NPC 私聊，不生成普通模式的六个选项和 HTML 美化框。

大型群聊开启后不能直接退回普通模式；已开启地图主线的角色不能切换到大型群聊。先选适合故事的形式，再让全剧组入场。

## 故事工作区与番外

聊天页点击「故事」，可以切换四个页签。

| 页签 | 内容 |
| --- | --- |
| 概览 | 前情摘要、当前目标、风险和最近时间线 |
| 人物 | NPC 的当前状态、关系与相关线索 |
| 线索 | 伏笔、行动建议和世界事件 |
| 番外 | 已保存的独立作品，以及同人文、NPC 日记、世界动态入口 |

这里整理的是已有剧情和生成结果，不保证能替你补齐模型从未写过的事实。发现摘要不准确时，可以结合原消息和长期记忆核对。

### 同人文工坊

从「故事」的「番外」页点击「同人文」。选择「我和 NPC」或「NPC 和 NPC」，挑选人物，填写灵感，或点击「开盲盒」。

一次生成消耗 10 啥币，模型 API 用量另外计算。过程中会显示流式预览，完成后弹出全文并保存到「历史同人文」；生成失败会退回这次扣除的 10 啥币。

同人文会参考人物和剧情资料，但属于独立番外，不推进主线、不更新主线游戏面板。NPC 日记、世界动态也可以从「番外」入口生成和查看，使用前留意界面提示。

## NPC 私聊与角色卡

### 从哪里找到 NPC

进入「NPC」，查看当前世界的人物。主线生成并被应用识别到的 NPC 会进入档案，也可以手动建立人物。聊天正文提到一个名字，并不一定已经形成可私聊的档案。

如果“故事里见过，但列表没有”，先检查是否切到了另一个剧场，以及该人物是否已保存为 NPC。世界书里写下名字，也不会自动等于创建 NPC。

### 私聊与关系

点开 NPC 进行独立私聊。对话会参考人物简介、印象、关系和相关剧情。输入栏提供送礼物、查看心声等操作，NPC 也可能在剧情条件满足时主动发消息。主动消息不代表现实中的定时在线服务。

主线与私聊可以沉淀羁绊阶段、路线、关键词和节点记录。数值好感与文字印象分别保存，二者可能一起影响后续表现。赠礼和功能券的消耗以操作界面为准。

### 编辑、补全和绑定

NPC 编辑器可以修改名字、简介、印象、好感、头像、生命周期和角色卡。AI 补全会显示预览，可以选择「应用到空字段」或「全部应用」，确认后再保存。

需要让人物作为同行 NPC 或重要配角参与主线时，先补全 NPC 角色卡，再设置绑定。打开「全局绑定」会让其跟随进入所有模拟器；关闭时至少选择一个指定世界。

绑定 NPC 可以表达意见并参与剧情，玩家仍然只操控自己的角色。不要把绑定理解为把别的世界整份聊天记录一起搬过来。

## 带走 NPC

在 NPC 的相关操作中选择带走流程。你可以先告别，也可以跳过，不需要为了迁徙强行给旧世界补一场离别大戏。

1. 选择生成「可游玩新世界」或「只生成世界观」。也可以只整理 NPC 角色卡。
2. 设置叙事人称、新世界类型和灵感。
3. 选择旧世界记忆强度，包括完整记得、片段记得或只留熟悉感。
4. 设置关系路线，按需选择前尘信物、关系任务、旧世界回声和是否写入告别。
5. 检查生成预览中的新设定、角色卡、开场白及其他内容，再确认保存。

生成新世界和整理世界观的结果不同。前者可以继续游玩，后者主要保存前尘设定，不会凭空多出一段已经演完的新剧情。旧世界与新世界的内容可以通过迁徙档案回看。

## 用户人设与世界书

### 用户人设

「角色」管理故事和 AI 设定，「用户」管理你在故事里扮演的人物。在「用户」里点击「新建用户」，填写名称、简介和「用户人设」，按需上传头像、绑定剧场。

可以建立多份人设，分别绑定不同故事。不绑定也能正常游玩；绑定后，模型会在后续剧情中参考你的身份、说话方式、目标和边界。

### 世界书

从「角色」页进入「世界书」，点击「新建条目」。填写标题、内容和标签，选择注入位置、优先级，以及「全局生效」或指定绑定角色，再保存。

适合放进世界书的内容包括世界规则、人物关系、固定地点、文风偏好和内容边界。已经发生的重要剧情，通常更适合长期记忆。

世界书支持按标签筛选和批量管理。全局条目会影响多个剧场，给某个故事写的规则不要不小心绑定给所有人。条目较长时可以展开阅读；内容越多，传给模型时也可能占用更多上下文。

## 长期记忆与生成设置

### 查看和修改记忆

角色详情中的「查看长期记忆」可打开记忆库。你可以新增、编辑或删除摘要。长期记忆保留重要剧情，不会无限记住全部原文；删除摘要也不会删除原始聊天消息。

在「设置」的「记忆保存策略」里调整自动总结触发阈值和注入模型的长期记忆条数，修改后点击「保存记忆策略」。总结会调用 AI，较频繁的总结可能增加调用次数。

需要分开使用服务时，填写完整的「记忆总结专用 API」配置，测试后保存；留空时使用主 API。不要把填写了一半的配置当成可用的独立服务。

### 上下文与缓存

「API 设置」里的「Prompt Token 预算」控制发给模型的输入预算。当前自动模式采用 64K 目标预算；如果服务商模型支持的上下文更小，应手动调小，并给输出留出空间。设置预算不会扩大模型本身的能力。

当前版本会按阶段整理较长对话，并利用服务商支持的缓存。修改过的长期记忆会按新内容参与后续请求，重新回复也会尽量复用已有前缀；能否命中、如何计费，以服务商返回为准。

生成用量和诊断信息可帮助查看输入、输出、耗时及缓存情况。没有真实用量返回时，估算不能当作账单。兼容接口不支持某些流式统计时，可关闭相应统计选项再测试。

「请求超时」适合根据服务响应速度调整。延长超时只能多等一会儿，不能修好错误的 Key、模型名或服务地址。

### 预设

「设置」的「预设」可以保存当前 API、记忆策略和显示交互配置，之后切换使用。预设可能包含连接信息，分享存档时也要留意密钥是否被包含。

## 啥币与小游戏中心

啥币是本地游玩资源，可以从签到、每日任务、成就和版本福利等方式获得，用于游戏内商品和装扮。它不是真实货币，不能交易、提现，也不能支付 API 费用。

「小游戏中心」有今日、商店、唱片机、背包、装扮、成就六个页签。

- 「今日」查看奖励、任务和陪伴进度，完成后记得领取。
- 「商店」提供功能券、剧情商品和其他小游戏，购买前查看价格与效果。
- 「背包」区分功能券和剧情物品。不同物品的使用方式不同，有些生成独立小剧场，有些会投入主线。支持批量管理，删除前确认勾选范围。
- 「装扮」管理称号和气泡等外观。
- 「成就」查看已获得的记录与可领取奖励，隐藏成就在解锁后才会揭晓。

### 关于唱片机

**当前没有内置音乐包，也不分发音乐素材；本地音频功能仍保留。** 在「唱片机」里可以「上传音频」，管理音频名称、播放、切换和歌单。扩展音频格子会消耗啥币，以按钮提示为准。

请只导入自己拥有或获授权的音频。浏览器可能要求先点击或拖拽页面才允许播放，自动播放没有响时可以手动点击播放。换设备前不要假定本地音频文件一定随剧情存档完整迁移。

## 主题与气泡

在「设置」的「显示与交互」中调整界面缩放和 UI 主题，完成后点击「保存显示与交互」。手机省电相关选项可以减少动态效果和刷新负担。

需要啥币解锁的主题或格子，以当前界面显示的价格为准。自定义主题可以选择已有主题为底稿，再用支持的 CSS 变量调整样式。

在「小游戏中心」的「装扮」中预览气泡皮肤，然后全局装备或绑定当前角色。角色专属气泡优先于全局设置，想恢复全局样式时取消当前角色的气泡绑定。

自定义气泡支持导入或粘贴 CSS 子集，不是完整网页浏览器的全部 CSS 能力。先在预览里确认可读性和手机宽度下的表现，再保存。

## 备份迁移与恢复

### 选择合适的导出

在「设置」的「存档迁移」中操作。

| 按钮 | 用途与注意事项 |
| --- | --- |
| 导出全部数据 | 默认不包含 API Key，适合常规备份和迁移，但仍包含私人剧情 |
| 导出完整数据 | 包含 API 密钥，限自己妥善保存，不要公开分享 |
| 导出当前角色 | 导出当前剧场的可迁移数据，适合单个故事备份 |
| 导入数据 | 选择存档文件或粘贴内容，先检查预览再确认 |

默认导入方式是合并。选择「覆盖当前数据」会用导入存档替换当前相关数据，覆盖前请先导出备份。过大的剪贴板内容应改用文件导入，存档文件限制为 64MB。

当前存档包含版本、时间和校验信息，并兼容旧格式导入。不要随意修改导出 JSON 的内容，否则可能无法校验或恢复。文件名中出现旧版字符串，也不等于存档内容就是旧版，以导入预览为准。

### 恢复中心

「恢复中心」汇集手动存档、自动保护和已删除剧场的快照。当前剧场可以命名并保存一个保存点，也能查看已有快照、恢复或删除；另有「生成当前进度封面」入口。

| 方式 | 适合什么时候用 |
| --- | --- |
| 手动快照 | 大改设定、重做玩法或重要剧情选择前 |
| 自动保护快照 | 清空、删除或覆盖后，需要查看操作前的状态 |
| IF 线 | 想从某个节点另开一条独立剧情 |
| JSON 导出 | 更换设备、卸载应用或长期保存备份 |

恢复快照会将相关进度还原到保存点，请先保留需要的当前进度。自动保护会增加找回机会，但不能保证所有数据都可恢复；浏览器数据或应用沙盒被清空后，本地快照也可能一起丢失。

### 数据保险箱

「数据保险箱」用于查看占用、检查断裂绑定等存档健康问题，以及清理指定类别的数据。可修复的问题会提供「一键修复」入口；修复前阅读提示。

遇到损坏原文被隔离时，可以复制或导出原文后再处理。删除隔离原文后，应用内无法找回这份原始内容。清理剧情工具历史、同人文、NPC 私聊或地图 HTML 缓存时，也请逐项确认，不要为了省空间把还想看的剧情顺手扫走。

## 数据保存与隐私

角色、聊天、世界书、存档和设置主要保存在当前设备。网页存档跟浏览器及网站来源绑定，协议、域名或端口变化，都可能进入一个不同的存储空间。固定本地端口和正常浏览器可以减少“怎么又是第一次开幕”的困惑。

清理浏览器数据、使用无痕窗口或卸载应用，可能失去本地内容。应用没有自动云同步，换设备前请主动导出。

**本地保存不等于离线生成。** 调用模型时，所需对话、设定和记忆会发给你配置的 AI 服务商。API 设置也会保存在设备上，共用电脑时请注意账户和浏览器的使用范围。

给别人分享前，检查截图、剧情、用户人设及存档内容。默认导出去掉 Key，不会自动替你删掉故事里写过的个人信息；「导出完整数据」还会带上密钥。

## 遇到问题先看这里

| 现象 | 先检查什么 |
| --- | --- |
| 点击发送后 AI 不回答 | 普通发送只加入本回合，点击小飞机提交生成 |
| 测试连接失败 | 地址、Key、模型名称、服务状态和额度；网页端还要检查跨域支持 |
| 测试成功，回聊天仍提示未配置 | 是否点击了保存 API 设置 |
| 生成预览只出现半段 | 等待生成、补全或修复结束；失败时保留脱敏后的报错 |
| 回复看起来忘了前情 | 当前剧场是否正确，记忆是否准确，输入预算是否适合模型 |
| 角色设定被忽略 | 检查角色提示词、绑定用户、世界书是否相互矛盾，再修正相关内容 |
| NPC 列表里找不到人 | 是否在当前世界，是否真的建立了 NPC 档案，而非只在正文提到名字 |
| 同人文没出现在主聊天 | 到故事的番外和历史同人文查看，它不推进主线 |
| 找不到用户入口或顶部按钮 | 窄屏布局会收起部分入口，查看聊天页用户按钮和功能菜单 |
| 换浏览器或地址后没有旧进度 | 回到原浏览器和原地址导出，再到新位置导入 |
| 页面卡在启动阶段 | 阅读启动状态或故障提示，尝试重试；不要先清空浏览器数据 |
| 主题或气泡改了却没生效 | 是否保存，当前角色是否还绑定了专属气泡 |
| 安卓不能覆盖安装 | 新旧包签名可能不同，先在旧版导出，再处理卸载和重装 |

反馈时带上应用版本、设备与浏览器、复现步骤、脱敏后的错误信息。需要讲清楚“点了什么、期待什么、实际发生什么”，不用把整段私人存档发到公开 Issue。

## 转载修改与交流

欢迎非商业转载、修改、再发布，也欢迎来 [GitHub Issues](https://github.com/4433shijue/unfinished-theater/issues) 反馈问题、讨论玩法或分享你的改动。

**原版、修改版和二次创作版本都禁止商用。** 再发布时保留来源和许可，不能通过修改或换名获得商业使用权。完整条件见 [非商业使用许可](LICENSE.md)，第三方字体与素材仍按各自的授权处理。
