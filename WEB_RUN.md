# 网页运行说明

环境准备、启动和构建命令统一见 [README](README.md#下载源码本地开演)。

Windows 运行 `run_web.ps1`，macOS/Linux 运行 `sh ./run_web.sh`。默认地址固定为 `http://127.0.0.1:5173`，请用日常浏览器打开并保持终端运行。

Web 构建输出为 `build/web/`，需要 HTTP 服务，不能双击 HTML。部署 GitHub Pages 和发布安装包见 [发布指南](docs/PUBLISHING.md)。
