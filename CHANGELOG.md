## 网页启动优化 · 2026-09-11

- 启动必需字体从约 89 MB 原文件缩减为约 3.4 MB 基础字体；主题字体在首屏之后按需加载。
- 基础字体随应用提供，启动无需额外下载默认 Roboto 字体。
- 启动页显示当前阶段，分别提示慢加载和加载失败；主题字体重试不刷新页面、不清除存档。

# 更新记录

## V2.10.5 重点

- HTML 美化框协议去重：完整规则只保留在角色运行时协议一处（每轮格式提醒与最终输出复核改为引用），固定前缀精简约 200-300 字，指令冲突减少、格式修复触发率预期下降。
- HTML 面板要求精简：去掉"内容区域允许独立滚动"，保留单文件、适配手机、不引用外部资源。

## V2.10.4 重点

- 「AI 帮你写」改为结构化 JSON 输出：生成结果自动拆进名称、简介、开场白、系统提示词四个输入框，提示词框只保留纯系统提示词正文，不再带标题头。
- 生成过程显示滚动实时预览区（可选中复制），JSON 可解析时按【名称】【简介】【开场白】【系统提示词】分段展示，完成后再拆分填入；开盲盒与润色同样支持。
- 兼容旧格式：遇到仍按「模拟器名称：…」标签输出的模型，自动续写修复并回退到旧解析方式。
- 帮你写指令整体去说明书味：从"必须/禁止"清单改为编辑给作者的口吻，生成内容更少模板腔。

## V2.10.3 重点

- 「AI 帮你写」全家桶改为流式输出：模拟器生成、开盲盒、润色会边生成边填入提示词框；NPC 角色卡生成有实时预览；同人文生成有流式弹窗，完成后自动展示全文。
- 截断自动续写：生成结果缺段（帮你写缺四段之一）或明显过短（同人文缺标题/正文过短）时，自动发起一次续写请求补全后再解析，不再把半成品直接甩给用户。
- 所有工具生成统一显式设置输出上限（默认 8192 tokens），长内容不再被模型默认上限截断。
- 玩法系统 JSON 解析失败时自动携带原文修复一次；仍失败时错误信息会附带模型原始输出片段，方便排查。
- 生成失败兜底：同人文失败仍退回 10 啥币；NPC 角色卡解析失败时原文会作为角色卡草稿保留，不会直接丢失。
- 修复 NPC 私聊输入栏消失：NPC 私聊页输入栏不再与主聊天共用教程定位 key（重复 GlobalKey 会导致压栈页面的输入栏被截断），发送键与小飞机恢复正常。

## V2.10.2 重点

- 长期记忆按「id+内容摘要」版本键跟踪：编辑过的记忆会在下一轮自动重新注入，不再需要等缓存阶段换代。
- 重新回复不再强制重置缓存阶段：请求前缀与上一轮一致时可直接命中服务端上下文缓存，长对话重生成更省。
- 剧情检查点从固定前缀移到请求尾部：阶段换代时协议、人设、世界书和窗口内历史继续命中缓存，只重发检查点与最新回合。
- 检查点优先收录最近的记忆，且只把真正写入检查点的内容标记为已注入；超量记忆与世界书会继续随本轮快照注入，不再静默丢失。
- 当固定上下文（协议、检查点、世界书、记忆与状态面板）超出 Prompt 预算时，故事洞察页会提示精简或调大预算，避免缓存阶段反复重建。
- Token 预算估算改为按实际消息窗口匹配的世界书与记忆计算，换代判断与真实请求更一致。
- 自动 Prompt 预算统一为 64K：非 DeepSeek 接口不再使用保守的 8.4K 默认值，与 DeepSeek 官方接口一致；模型上下文偏小时可在设置页手动调小。

## V2.10.1 重点

- DeepSeek 官方接口按安装 ID 隔离 KV 缓存，并保存每轮缓存命中、未命中和上一轮前缀覆盖率。
- 固定协议、角色/用户/NPC 稳定资料和常驻世界书保持稳定前缀；动态状态只在首次需要时注入，减少无效 cache miss。
- 长对话按 cache epoch 滚动：超出消息数或 Prompt Token 预算时生成阶段检查点，并从完整的用户/助手轮次边界开始新前缀。
- DeepSeek 历史助手消息优先逐字回放供应商原始输出；编辑或修复过的内容自动退回安全的压缩回放。
- 设置页新增 Prompt Token 预算。自动模式在 DeepSeek 官方接口使用 64K，其他 OpenAI 兼容接口使用保守预算。

## V2.10.0 重点

- 气泡皮肤升级为可伸缩结构与美术角饰的混合渲染，加入便签折角、猫爪、票根、裂隙、胶片齿孔和像素硬边等差异化效果。
- 装扮页为每个内置及自定义气泡提供真实消息预览，购买和绑定角色前即可检查实际效果。
- 首次教程从 25 步缩减为 API、剧场、首回合和帮助入口四步，去除不可靠的屏幕方位箭头。
- 设置页新增分章节完整功能教程；流式用量统计改为默认开启，仍允许为不兼容中转手动关闭。

## V2.9.0 重点

- 角色创建后可一键生成只属于当前剧本的玩法变量 Schema，不再把生命、好感或金币硬编码给所有题材。
- 变量支持公开、模糊、导演和引擎四级可见性；玩家面板保持沉浸，创作者可在幕后参数中查看完整设计。
- AI 每轮通过 `[THEATER_PATCH]` 提交变化，本地只接受已声明、授权且符合类型与范围的操作。
- 个性变量保存在现有游戏状态和消息快照中，兼容导出、恢复、剧情分支与旧存档。
- 新增玩法 Schema 解析、权限收紧、变量限幅、补丁隐藏和快照序列化测试。

## V2.8.5 重点

- Web 会先显示原生启动状态并使用包内 CanvasKit，再初始化本地存储和音频插件，慢设备也能立即看到可见状态。
- 数据初始化新增阶段提示，覆盖应用设置、角色与世界资料、存档校验和剧情现场恢复。
- 平台或本地数据初始化失败时不再无限加载；故障页会保留存档并提供直接重试。
- 故障页支持复制版本、失败阶段、错误类型和堆栈诊断，方便定位问题。
- 新增启动等待、平台失败重试和数据初始化恢复的回归测试。

## V2.6.1 重点

- NPC 私聊列表会同时识别当前角色、根角色、分支绑定和跨世界绑定 NPC，避免状态里有 NPC、列表里看不见。
- `[GAME_STATE]` 里的 `NPC变化` 只作为状态面板文本；`NPC更新` 才创建或更新 NPC 档案。
- `主动消息` 会先过滤“等待回复中、明日将联系、信已传递”等状态句，不再把状态塞进 NPC 私聊气泡。
- 补充构建加强 JSON 形态 `NPC更新` 的中文键解析，并减少真聊天被状态过滤误伤。
- 格式修复器和主线回合提示词明确区分 NPC 状态、NPC 档案和 NPC 私聊原话。
- V2.6.1 修复福利通过邮箱发放 30 啥币。

## V2.6 重点

- 数据导出升级为 V2 存档封套：带 schema、版本号、时间戳和 checksum，同时保持旧 JSON 存档可导入。
- AI 回复统一经过请求上下文、token 预算和 trace 管线，流式暂停也在同一链路内处理。
- NPC 会在剧情状态更新时沉淀运行状态、关系边、短记忆和世界事件，后续跨轮连续性更稳。
- 新增存档封套、token 估算和 NPC runtime 的回归测试。
- V2.6 更新福利通过邮箱发放 70 啥币。

## V2.5.5 重点

- 聊天气泡底部新增近似 token 估算：用户消息显示本条输入估算，AI 回复显示本轮实际请求上下文估算。
- AI 回复里的 `[CHOICES]` 行动选项默认折叠，点击「展开」后再显示完整选项，长聊天回看更省空间。
- AI 流式回复期间，发射键会切换为暂停键；误触或想中断时再点一次即可暂停当前回复。
- V2.5.5 更新福利通过邮箱发放 30 啥币。

## V2.5.4 重点

- 地图页新增「主线罗盘」：集中展示当前阶段、目标、时间、地点和推荐下一步。
- 当前地点新增状态卡：状态、风险、耗时、相关 NPC、线索和地点可做行动放在一处查看。
- 地图协议新增 `npcPositions`：AI 会回填重要 NPC 的地点、状态、意图和最后出现时间，地图页直接展示 NPC 位置。
- 地图输出提示词补强：继续维护 `currentScene`、`locations`、`activeChoices`、线索和 NPC 动向，让地图主线更像剧情导航仪。
- V2.5.4 更新福利通过邮箱发放 30 啥币。

## V2.5.3 重点

- 六套基础主题保留原有配色，整体升级为杂志式 UI：更克制的边框、8px 左右的卡片半径、衬线标题和更适合长剧情阅读的气泡层级。
- 聊天页新增「剧情分享卡」入口，可把最近剧情生成一张主题化 HTML 卡片，方便打开后截图或分享。
- 导出模式补强：选中若干消息后，除了导出完整 HTML，也能只把选中片段生成剧情分享卡。
- 基础主题字体分工调整：标题和剧情正文更有出版物质感，按钮、输入框和导航仍保持清楚好操作。
- V2.5.3 更新福利通过邮箱发放 30 啥币。

## V2.5.2 重点

- 底部「NPC」入口回归「NPC 私聊」语义：只显示当前世界 NPC，不再把全局绑定或其他世界的 NPC 角色卡混进来。
- 已整理成角色卡但来源属于当前世界的 NPC 仍会在当前世界 NPC 私聊里出现，也能正常聊天、查看和继续带走。
- 新建 NPC 增加「AI 帮你写」：用户填一点人物灵感，AI 会生成名字、简介、初始印象和完整角色卡。
- Web 启动减负：本地存档备份不再阻塞开场加载，改为启动后延迟刷新，公网部署白屏时间会更短。
- 背景音乐资源重新压缩，约从 69.85MB 降到 60.52MB；音乐加载超时提示改为可稍后重试，不影响聊天和存档。
- V2.5.2 修复福利通过邮箱发放 30 啥币。

## V2.5.1 重点

- 底部功能栏新增「NPC」入口，NPC 角色卡从角色页挪到独立 NPC 页；原世界「NPC 私聊」保留，聊天不受影响。
- NPC 角色库去重：同一个 NPC 在创建分支或整理角色卡后不会重复显示，旧存档加载时也会合并重复卡。
- NPC 完整角色卡可编辑：角色卡详情里新增编辑入口，可修改角色卡正文、简介、印象和绑定信息。
- 创建分支会读取分支点附近的历史状态快照；没有快照时从分支内历史重新解析，避免把结局后的 NPC 和状态面板带回早期 IF 线。
- 森林莓晨露改为莓果粉主色调，晨露绿辅助，和鼠尾草薄荷明显区分。
- 「只生成世界观」改成设定集导向，重点写世界运行逻辑、规则、势力、文化、冲突和进入接口，而不是模拟器玩法壳。
- V2.5.1 修复福利通过邮箱发放 50 啥币。

## V2.5 重点

- NPC 私聊输入框左侧新增「+」工具菜单，可送背包剧情物品，也可购买神秘小卖部或黑心小卖部商品直接送给 TA，送礼会影响好感和印象。
- NPC 私聊新增「听听 TA 的心声」：每个 NPC 回复气泡可生成一次心声，也可手动重新生成当前心声。
- NPC 角色库修复：补全角色卡后仍能私聊、编辑、查看详情、绑定到其他世界，不再只是只读简介。
- 带走 NPC 可选择「可游玩新世界」或「只生成世界观」；新世界提示词更贴近「AI 帮你写」模板，状态面板字段和普通文游保持一致。
- 创建角色「帮你写」支持角色名留空，只填一句话灵感；帮你写和自己写都支持上传 md/json/doc/docx/txt 文本文档生成或填充提示词。
- 主聊天和 NPC 私聊重新回复会弹出方向输入框，输入内容会加入隐藏提示词；剧情分支会读取对应历史消息附近的状态快照。
- 同人文开盲盒改为直接生成，不展示内置题材列表，并要求生成标题、独立番外，不照抄当前剧情。
- 世界书移除「触发方式」配置，长期记忆支持手动新增，设置目录跳转、前尘档案馆滚动和前尘回声触发同步修复。
- 鼠尾草薄荷与森林莓晨露配色拉开差异，V2.5 版本福利通过邮箱发放 70 啥币。

## V2.4.2 重点

- 手机端全局换行优化：角色页、NPC 库、迁徙弹窗、零件市集、唱片机、设置页、世界书、背包和导入预览减少省略号，标题与关键说明优先换行。
- 角色页曾新增「我的角色」和「NPC 角色库」分区；V2.5.1 起 NPC 角色卡统一进入底部「NPC」页。
- NPC 详情新增「整理成 NPC 角色卡 / 重新补全」入口，AI 只补全干净人设，不生成新世界、不推进剧情、不写告别，并去掉原世界强绑定背景。
- 绑定到世界升级为角色卡前置流程：没有角色卡会先提示补全；绑定后作为可自主行动的同行 NPC / 重要配角注入目标模拟器。
- 设置页目录补全：API、记忆、显示、预设、存档和说明全部可点击跳转，目录按钮在手机端可换行并显示当前高亮。
- 唱片机手机端重排：当前播放信息纵向展示，上一首/播放/下一首独立成行，长歌名和下拉选歌可以多行显示。
- V2.4.2 手机端适配、NPC 角色库、设置目录补全版本福利通过邮箱发放 50 啥币。

## V2.4.1 重点

- 新增开场帷幕层：桌面端鼠标拖拽、手机端手指拖拽，拉开后进入正式界面。
- 开场白改为剧场式仪式文案：故事从用户亲手拉开帷幕开始。
- 开幕后自动尝试播放当前唱片机曲目，利用拖拽手势满足浏览器音乐播放限制。
- Web、PWA 和 Android 共用同一套 Flutter 绘制开场层。
- V2.4.1 开幕帷幕版本福利通过邮箱发放 41 啥币。

## V2.4 重点

- 新增全局搜索入口：桌面和移动端都可从顶部标题栏打开，统一搜索角色、主聊天、书签、NPC 私聊、世界书、工具结果、同人文和前尘档案。
- 聊天页新增「继续上次剧情」看板：回到当前角色时能直接看到上次停在哪、当前任务、未读 NPC、未领邮箱、书签数量和地图提示。
- 聊天消息新增书签与备注：桌面悬停或手机长按消息即可加入书签，重要告白、伏笔、选择点、线索都能标记。
- 自动保护快照上线：清空聊天、删除角色、删除/批量删除消息、导入覆盖前会自动创建本地保护快照，最多保留最近 12 个自动保护点。
- 设置页新增目录导航：不用再一路滚，直接跳到 API、记忆、显示、预设、存档迁移或使用说明。
- V2.4 总控台版本福利通过邮箱发放 100 啥币。

## V2.3.1 重点

- NPC 私聊页升级为「NPC 库」：同一个 NPC 可以拥有完整角色卡，不再只属于单个世界里的临时档案。
- 手动创建或编辑 NPC 时，可开启“作为另一个主角进入剧情”，并选择绑定当前模拟器、多个模拟器或全局绑定。
- 绑定 NPC 会在主线提示词里作为自主行动角色注入：TA 能行动、提议、推动支线，但不能替玩家做决定。
- 「带 TA 走」新增第二条路径：除了生成专属新世界模拟器，也可以只生成 NPC 角色卡，再绑定到别的世界一起玩。
- 「AI 帮你写模拟器」新增“完整模拟器 / 只生成世界观”两档；世界观模式适合带着玩家和绑定 NPC 进入新舞台。
- V2.3.1 NPC 角色卡版本福利通过邮箱发放 50 啥币。

## V2.3 重点

- 「带走 NPC」新增旧世界告别：可跳过、只留一句、自己写，或让 AI 生成一场迁徙前的最后回合。
- 再续前缘配置新增关系路线锁：跟随旧羁绊、恋人、挚友、宿敌、共犯、守护、师徒、破镜、不恋爱化或自然变化。
- 角色页新增「前尘档案馆」入口，可查看来源、告别、迁徙档案、世界书、角色卡、开场白、信物、任务、回声和纪念册。
- 角色列表新增前尘新世界分组，带走到新世界的 NPC 不再混在普通角色里。
- 迁徙成功后会保存前尘信物和关系任务；迁徙角色可触发前尘回声，把旧世界自然带回当前剧情。
- 前尘档案支持指定部分 AI 重修，并可把重要片段加入双人纪念册。
- V2.3 前尘档案馆版本福利通过邮箱发放 120 啥币。

## V2.2 重点

- 「带走 NPC」改成两步确认：先生成迁徙预览，再由用户确认创建，避免 AI 跑偏后直接污染角色列表。
- 迁徙预览会展示旧世界来源摘要、迁徙档案、前尘世界书、新角色卡和开场白。
- 旧世界记忆从开关升级为三档：完整记得、片段记得、只留熟悉感。
- 每次迁徙都会保存迁徙记录，旧 NPC 卡片会显示已带去的新角色，新角色列表也会标记“NPC 迁徙”。
- 导出导入会保留 NPC 迁徙链路，换设备后仍能知道新角色来自哪个旧世界 NPC。

## V2.1 重点

- 小游戏中心新增「剧场唱片机」，背景音乐可播放、暂停、上一首、下一首、调音量，并支持单曲循环和歌单循环。
- 内置 14 首主题曲，对应当前全部 UI 主题；六首基础主题曲默认解锁，其余主题曲 70 啥币永久解锁。
- 歌曲支持横向滑动卡片选择，也提供下拉列表快速切歌；付费歌曲卡片会展示解锁状态和主题特色文案。
- 已解锁歌曲可加入自定义歌单，支持移出、播放和拖拽排序，歌单会随本地数据保存和导入导出。
- 新增音乐播放、暂停、切歌、购买、歌单、播放时长和主题曲隐藏彩蛋成就；V2.1 更新福利通过邮箱发放 70 啥币。

## V2.0.5 重点

- 聊天消息解析增加轻量 LRU 缓存，长记录来回滚动时不再反复拆同一段 Markdown、选项和 HTML。
- 较早 AI 回复里的 HTML 美化框默认折叠，只保留最近几条自动渲染；用户可点击展开或直接全屏查看。
- 导入存档预览支持后台解析大 JSON，减少 2-3MB 文件导致网页版卡住的情况。
- 数据清理器改为直接统计本地存储 key 大小，不再为了估算 MB 重新拼完整导出包。
- V2.0.5 性能专项福利通过邮箱发放 50 啥币。

## V2.0.4 重点

- 「带走 NPC」改为三段 AI 生成：先整理旧世界档案，再生成专属世界书，最后生成可直接游玩的文游模拟器角色。
- 迁徙弹窗新增叙事人称选择：第二人称、第一人称、第三人称都会写入新角色提示词和世界书。
- 带走后的新角色会继续要求纯文本剧情、HTML 美化框、`[GAME_STATE]` 和 `[CHOICES]`，故事主体只围绕你和 TA 展开。
- 迁徙角色卡提示词改为分段结构，补全 NPC 设定、关系事实、旧世界记忆、核心玩法循环、状态系统和首回合规则。
- 世界书列表改为按主标签展示，筛选标签时才进入对应标签组，避免「你们的前尘」在多个分组里重复出现。
- V2.0.4 修补更新福利通过邮箱发放 50 啥币。

## V2.0.3 重点

- 应用正式改名为「未完剧场」，App 标题、PWA 名称和 Android 显示名同步更新。
- 设置页新增「自定义主题工坊」：200 啥币购买一个自定义主题格子，可选择已有主题作为底稿。
- 自定义主题支持 CSS 变量覆盖、示例复制、填入示例、导入 `.css/.txt` 文件和实时预览。
- 自定义主题可直接保存、编辑、删除和切换；未覆盖的变量会继承底稿主题。
- 桌面端顶部工具栏新增横向滚动条，按钮再多也不会被挤出屏幕。
- 新增自定义主题相关成就和称号「主题裁缝」，并发放 V2.0.3 小版本 50 啥币福利。

## V2.0.2 重点

- 装扮页新增「自定义气泡格子」：50 啥币购买一个格子，可填写气泡名称、介绍和 CSS 子集样式。
- 自定义气泡编辑器内置示例 CSS，支持复制示例、填入示例、导入 `.css/.txt` 文件和实时预览，方便用户自己改或交给别的 AI 生成。
- 气泡边框支持“全局装备”和“绑定当前角色”，主聊天按角色专属优先，全局次之。
- NPC 私聊气泡也接入边框装扮和自定义气泡，不再只有主聊天能换装。
- 新增自定义气泡相关成就，并发放 V2.0.2 小版本 50 啥币福利。

## V2.0.1 重点

- 修复 NPC 私聊列表在手机端的羁绊路线显示：卡片改用窄屏资料卡布局，状态胶囊独占更稳定的换行空间。
- 羁绊胶囊会自动收起“未知线”，长文本也会省略显示，不再把胶囊撑破。
- 新增静态预览文件 `气泡边框预览-V2.0.1.html`，集中展示当前全部气泡边框的名字、样式和重构候选效果。
- V2.0.1 小版本福利通过邮箱发放 50 啥币。

## V2.0.0 重点

- NPC 私聊页新增「带 TA 走」：整理旧世界印象、私聊片段和羁绊路线，让 AI 补全角色卡，生成只属于你们的新世界。
- 新增「羁绊路线」：NPC 档案会显示阶段、路线倾向、进度、关键词和节点历史；主线与私聊都会继续推进羁绊。
- 带走 NPC 会自动创建新 AI 角色，并绑定「你们的前尘」世界书，保留旧世界记忆或只保留熟悉感都可选。
- NPC 私聊后的印象显示清理英文格式，只保留简体中文表达。
- 新功能界面继续跟随当前主题，个性化 UI 也补了专属文案。

## V1.9.9 重点

- 新增 100 啥币主题「晴空牧场」和「黑胶往事」：晴空牧场使用小赖字体，黑胶往事使用思源宋体 + 霞鹜文楷，购买后首次装扮会触发专属成就。
- 裂隙中转站全局字体切换为字体传奇特战体，原有裂隙文案和彩蛋继续保留。
- 成就系统扩容：同人文、主题首装扮、存档、导入、数据清理器、世界书批量绑定等新功能都补上成就，隐藏成就解锁后会标注“隐藏”。
- 补发 V1.9.8 更新福利，并新增 V1.9.9 版本福利邮件。
- 手机端游戏状态面板支持长文本换行，角色卡窄屏只显示头像、角色名和一句简介，减少挤压。

## V1.9.8 重点

- 花非花、机械迷城、雨巷电台三套需要啥币解锁的个性化 UI 完成重构，保留各自特色文案。
- 花非花接入文鼎 PL 简中楷体作为预置字体，随项目资产一起打包。
- 聊天背景加入主题动态层：机械迷城是齿轮和蒸汽，花非花是水墨花影和花瓣，雨巷电台是分布式随机雨点和水纹。
- 宽屏侧边栏针对三套个性 UI 调整为更有场景感的横向按钮布局。

## V1.9.7 重点

- 小游戏中心「背包」新增批量管理，功能券和剧情物品都可以单选、全选、分类全选并删除。
- 设置页新增「存档快照」，用于给当前角色保存/恢复本地保存点；IF 线仍然是平行剧情分支。
- 导入数据前会预览角色、消息、NPC、世界书、剧情工具、同人文等概要，减少误导入。
- 设置页新增「数据清理器」，显示当前数据约多少 MB，并支持清理常见生成内容缓存。
- 世界书新增标签字段，列表按标签分组，支持筛选、批量绑定、批量设全局和批量删除。
- 六套基础 UI 去掉霓虹色，重构为更清新的低饱和莫兰迪渐变：雾粉紫、杏桃日落、雾蓝破晓、鼠尾草薄荷、月柠浅雾、森莓晨露；V2.5.1 中森莓晨露调整为粉色主调加绿色辅助。
- V1.9 起已移除内置试玩 API，使用前需要配置自己的 API 地址、Key 和模型。
